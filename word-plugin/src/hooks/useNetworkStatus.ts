/**
 * Re-export the useNetworkStatus hook from NetworkStatusContext
 * This provides a cleaner import path for consumers
 */
export { useNetworkStatus, type NetworkStatusContextValue } from '../contexts/NetworkStatusContext';
